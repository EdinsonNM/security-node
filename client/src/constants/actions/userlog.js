const moduleName = 'USERLOG_ACTIONS';

const USERLOG_ACTIONS = {
    LOAD: `${moduleName}_LOAD`,
    LOAD_OK: `${moduleName}_LOAD_OK`,
    LOAD_ERROR: `${moduleName}_LOAD_ERROR`
};
export default USERLOG_ACTIONS;
